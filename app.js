const button= document.getElementById('start');
let minutes=10;
let seconds;

////Usar función setTimeOut, que viene dada en milisegundos. 1.000

///10 minutos son 6.000 segundos. que son 6.000.000 de milisegundos                            xgggg

const countMinutes = () => {
    minutes--;
    setInterval()
};




///button.addEventListener('click', letsCountMinutes)


///Logica: cuando presione el botón, entonces, letscount va a aparecer en el DOM